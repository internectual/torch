# Torch configuration
dataDir = ~/t2-linux
# Output directory for compiled .dso, console.log, and other runtime files
# Defaults to ~/.loki/tribes2
# outputDir = /home/methodown/.loki/tribes2
# Preload files (comma-separated) - loaded after profiles, before main GUI loading
# preload = gui/ConsoleDlg.gui
# Preview image to show in bottom-right corner of dev panel
# previewImg = preview.png
initScript = console_start.cs
