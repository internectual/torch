# This file contains key bindings for Tribes 2 / Torque Game Engine.
# Primary bindings that should be in this file:
# - Pause: Bring up debug overlay (console/Pause trigger)
# Other bindings (like ~ for console) are handled by the GUI scripts

# Standard binding for debug overlay (Pause key)
bind "Pause" 0
